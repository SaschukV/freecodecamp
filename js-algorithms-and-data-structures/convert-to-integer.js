// ❓DESCRIPTION:
// Use parseInt() in the convertToInteger function so it converts the input
// string str into an integer, and returns it.

// ✅ Solution1:(моё решение)

function convertToInteger(str) {
  return parseInt(str)
}

console.log(convertToInteger('00056'))
